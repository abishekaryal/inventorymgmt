spring:
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 60000
  web:
    resources:
      static-locations: file:./uploads/
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB


  mail:
    host: smtp.gmail.com
    port: 587
    username: abishekaryal070@gmail.com
    password: wmkw lfsc guvd kcfz
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/changelog-master.yaml
    contexts: dev
  application:
    name: inventorymgmt
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: 'jdbc:mysql://localhost:3306/inventorymgmt_db'
    username: root
    password: Abishek60@
    jpa:
      hibernate:
        ddl-auto: none
      database-platform: org.hibernate.dialect.MySQLDialect
      show-sql: true
      database: mysql
      properties:
        hibernate:
          format_sql: true

  logging:
    level:
      org.hibernate.SQL: DEBUG
      org.hibernate.type.descriptor.sql.BasicBinder: TRACE
server:
  port: 8082
jwt:
  secret: a26ebe42e2c0d3c5b42005020f5060e75a22a067df8fef2a090dffe1b9a924c4
